#!/bin/bash

# --- set the name of the schroot ---
export MAP_TO_CHROOT=ubuntu1204
#------------------------------------
DIR="$( cd "$( dirname "$0" )" && pwd )" # get directory of current file
source ${DIR}/helpers.source
pathAdd ${DIR}
